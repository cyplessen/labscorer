% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{detect_timepoints}
\alias{detect_timepoints}
\title{Detect timepoints present in a data frame for a given scale}
\usage{
detect_timepoints(df, scale, timepoint_prefix = NULL)
}
\arguments{
\item{df}{A data frame.}

\item{scale}{Character; scale prefix (e.g. \code{"phq"}).}

\item{timepoint_prefix}{Character or \code{NULL}. The prefix used for timepoints
(e.g. \code{"t"}). If \code{NULL}, returns \code{""} (no timepoints).}
}
\value{
Character vector of timepoint labels, or \code{""} for cross-sectional
data.
}
\description{
Looks for columns matching the pattern \verb{<prefix><digit>_<scale>_*} and
returns the sorted unique timepoint labels (e.g. \code{c("t1", "t2")}).
}
\examples{
df <- data.frame(t1_phq_1 = 1, t1_phq_2 = 2, t2_phq_1 = 3, t2_phq_2 = 4)
detect_timepoints(df, "phq", timepoint_prefix = "t")
# [1] "t1" "t2"
}
